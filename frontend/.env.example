# Only needed when running the frontend outside Docker (`npm run dev`).
# Inside Docker these come from docker-compose.yml.

NUXT_PUBLIC_API_BASE=http://localhost:8000/api
NUXT_API_INTERNAL_BASE=http://localhost:8000/api
NUXT_PUBLIC_APP_NAME="Block Radar"
